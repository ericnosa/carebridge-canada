import { sqliteTable, text, integer, index, check } from 'drizzle-orm/sqlite-core';
import { sql } from 'drizzle-orm';
export const users = sqliteTable('users', { id: text('id').primaryKey(), createdAt: integer('created_at').notNull(), subject: text('subject').notNull().unique(), status: text('status').notNull().default('pending')}, t=>[]);
export const organizations = sqliteTable('organizations', { id: text('id').primaryKey(), createdAt: integer('created_at').notNull(), name: text('name').notNull(), status: text('status').notNull().default('pending')}, t=>[]);
export const clinics = sqliteTable('clinics', { id: text('id').primaryKey(), createdAt: integer('created_at').notNull(), organizationId: text('organization_id').notNull().references(()=>organizations.id), name: text('name').notNull()}, t=>[index('idx_clinics_org').on(t.organizationId),]);
export const roles = sqliteTable('roles', { id: text('id').primaryKey(), createdAt: integer('created_at').notNull(), name: text('name').notNull().unique()}, t=>[]);
export const permissions = sqliteTable('permissions', { id: text('id').primaryKey(), createdAt: integer('created_at').notNull(), name: text('name').notNull().unique()}, t=>[]);
export const memberships = sqliteTable('memberships', { id: text('id').primaryKey(), createdAt: integer('created_at').notNull(), organizationId: text('organization_id').notNull().references(()=>organizations.id), userId: text('user_id').notNull().references(()=>users.id), role: text('role').notNull(), clinicId: text('clinic_id').references(()=>clinics.id), status: text('status').notNull().default('pending'), expiresAt: integer('expires_at')}, t=>[index('idx_memberships_org').on(t.organizationId),]);
export const sessions = sqliteTable('sessions', { id: text('id').primaryKey(), createdAt: integer('created_at').notNull(), organizationId: text('organization_id').notNull().references(()=>organizations.id), userId: text('user_id').notNull().references(()=>users.id), membershipId: text('membership_id').references(()=>memberships.id), tokenHash: text('token_hash').notNull().unique(), mfaVerifiedAt: integer('mfa_verified_at'), expiresAt: integer('expires_at').notNull(), revokedAt: integer('revoked_at')}, t=>[index('idx_sessions_org').on(t.organizationId),]);
export const patients = sqliteTable('patients', { id: text('id').primaryKey(), createdAt: integer('created_at').notNull(), organizationId: text('organization_id').notNull().references(()=>organizations.id), clinicId: text('clinic_id').notNull().references(()=>clinics.id), ownerUserId: text('owner_user_id').references(()=>users.id), syntheticLabel: text('synthetic_label').notNull()}, t=>[index('idx_patients_org').on(t.organizationId),check('synthetic_patient_only',sql`${t.syntheticLabel} LIKE 'SYNTHETIC:%'`),]);
export const providers = sqliteTable('providers', { id: text('id').primaryKey(), createdAt: integer('created_at').notNull(), organizationId: text('organization_id').notNull().references(()=>organizations.id), userId: text('user_id').notNull().references(()=>users.id), profession: text('profession').notNull()}, t=>[index('idx_providers_org').on(t.organizationId),]);
export const providerCredentials = sqliteTable('provider_credentials', { id: text('id').primaryKey(), createdAt: integer('created_at').notNull(), organizationId: text('organization_id').notNull().references(()=>organizations.id), providerId: text('provider_id').notNull().references(()=>providers.id), regulator: text('regulator').notNull(), jurisdiction: text('jurisdiction').notNull(), registrationNumber: text('registration_number'), verificationSource: text('verification_source'), evidenceId: text('evidence_id'), verifiedAt: integer('verified_at'), expiresAt: integer('expires_at'), restrictions: text('restrictions'), status: text('status').notNull().default('Unverified')}, t=>[index('idx_provider_credentials_org').on(t.organizationId),]);
export const careRelationships = sqliteTable('care_relationships', { id: text('id').primaryKey(), createdAt: integer('created_at').notNull(), organizationId: text('organization_id').notNull().references(()=>organizations.id), patientId: text('patient_id').notNull().references(()=>patients.id), providerId: text('provider_id').notNull().references(()=>providers.id), clinicId: text('clinic_id').notNull().references(()=>clinics.id), authorityId: text('authority_id'), relationshipType: text('relationship_type').notNull(), startsAt: integer('starts_at').notNull(), endsAt: integer('ends_at'), status: text('status').notNull().default('pending')}, t=>[index('idx_care_relationships_org').on(t.organizationId),]);
export const patientRepresentatives = sqliteTable('patient_representatives', { id: text('id').primaryKey(), createdAt: integer('created_at').notNull(), organizationId: text('organization_id').notNull().references(()=>organizations.id), patientId: text('patient_id').notNull().references(()=>patients.id), userId: text('user_id').notNull().references(()=>users.id), authorityEvidence: text('authority_evidence').notNull(), expiresAt: integer('expires_at'), status: text('status').notNull().default('pending')}, t=>[index('idx_patient_representatives_org').on(t.organizationId),]);
export const consentRecords = sqliteTable('consent_records', { id: text('id').primaryKey(), createdAt: integer('created_at').notNull(), organizationId: text('organization_id').notNull().references(()=>organizations.id), patientId: text('patient_id').notNull().references(()=>patients.id), representativeId: text('representative_id'), recipient: text('recipient').notNull(), scope: text('scope').notNull(), purpose: text('purpose').notNull(), effectiveAt: integer('effective_at').notNull(), expiresAt: integer('expires_at'), withdrawnAt: integer('withdrawn_at'), policyVersion: text('policy_version').notNull(), version: integer('version').notNull(), evidence: text('evidence').notNull(), status: text('status').notNull().default('Declined')}, t=>[index('idx_consent_records_org').on(t.organizationId),]);
export const processingAuthorities = sqliteTable('processing_authorities', { id: text('id').primaryKey(), createdAt: integer('created_at').notNull(), organizationId: text('organization_id').notNull().references(()=>organizations.id), patientId: text('patient_id').references(()=>patients.id), basis: text('basis').notNull(), consentRequired: integer('consent_required',{mode:'boolean'}).notNull().default(true), recipient: text('recipient'), purpose: text('purpose').notNull(), scope: text('scope').notNull(), reviewer: text('reviewer'), evidence: text('evidence'), effectiveAt: integer('effective_at'), expiresAt: integer('expires_at'), status: text('status').notNull().default('Under Review')}, t=>[index('idx_processing_authorities_org').on(t.organizationId),]);
export const auditEvents = sqliteTable('audit_events', { id: text('id').primaryKey(), createdAt: integer('created_at').notNull(), organizationId: text('organization_id').notNull().references(()=>organizations.id), actor: text('actor').notNull(), role: text('role').notNull(), patientId: text('patient_id'), resource: text('resource').notNull(), action: text('action').notNull(), purpose: text('purpose').notNull(), authorizationResult: text('authorization_result').notNull(), sessionContext: text('session_context'), result: text('result').notNull(), correlationId: text('correlation_id').notNull()}, t=>[index('idx_audit_events_org').on(t.organizationId),]);
export const securityEvents = sqliteTable('security_events', { id: text('id').primaryKey(), createdAt: integer('created_at').notNull(), organizationId: text('organization_id').notNull().references(()=>organizations.id), category: text('category').notNull(), severity: text('severity').notNull(), correlationId: text('correlation_id').notNull(), status: text('status').notNull().default('open')}, t=>[index('idx_security_events_org').on(t.organizationId),]);
export const privacyRequests = sqliteTable('privacy_requests', { id: text('id').primaryKey(), createdAt: integer('created_at').notNull(), organizationId: text('organization_id').notNull().references(()=>organizations.id), requestType: text('request_type').notNull(), patientId: text('patient_id').references(()=>patients.id), owner: text('owner'), dueAt: integer('due_at'), status: text('status').notNull().default('open')}, t=>[index('idx_privacy_requests_org').on(t.organizationId),]);
export const incidents = sqliteTable('incidents', { id: text('id').primaryKey(), createdAt: integer('created_at').notNull(), organizationId: text('organization_id').notNull().references(()=>organizations.id), type: text('type').notNull(), severity: text('severity').notNull(), detectedAt: integer('detected_at').notNull(), affectedSystems: text('affected_systems'), affectedUsers: integer('affected_users'), phiInvolved: integer('phi_involved',{mode:'boolean'}).notNull().default(false), containment: text('containment'), rootCause: text('root_cause'), remediation: text('remediation'), reviewer: text('reviewer'), status: text('status').notNull().default('open'), notificationReview: text('notification_review').notNull().default('PRIVACY / LEGAL REVIEW REQUIRED')}, t=>[index('idx_incidents_org').on(t.organizationId),]);
export const clinicalContentRegistry = sqliteTable('clinical_content_registry', { id: text('id').primaryKey(), createdAt: integer('created_at').notNull(), organizationId: text('organization_id').notNull().references(()=>organizations.id), title: text('title').notNull(), sourceOrganization: text('source_organization').notNull(), sourceUrl: text('source_url').notNull(), jurisdiction: text('jurisdiction').notNull(), sourceVersion: text('source_version'), reviewer: text('reviewer'), approvedAt: integer('approved_at'), nextReviewAt: integer('next_review_at'), version: integer('version').notNull(), status: text('status').notNull().default('SOURCE VERIFICATION REQUIRED')}, t=>[index('idx_clinical_content_registry_org').on(t.organizationId),]);
export const clinicalPathways = sqliteTable('clinical_pathways', { id: text('id').primaryKey(), createdAt: integer('created_at').notNull(), organizationId: text('organization_id').notNull().references(()=>organizations.id), contentId: text('content_id').notNull().references(()=>clinicalContentRegistry.id), version: integer('version').notNull(), rules: text('rules').notNull(), reviewer: text('reviewer'), evidence: text('evidence'), status: text('status').notNull().default('Under Review')}, t=>[index('idx_clinical_pathways_org').on(t.organizationId),]);
export const connectorRegistry = sqliteTable('connector_registry', { id: text('id').primaryKey(), createdAt: integer('created_at').notNull(), organizationId: text('organization_id').notNull().references(()=>organizations.id), vendor: text('vendor').notNull(), jurisdiction: text('jurisdiction').notNull(), purpose: text('purpose').notNull(), dataTypes: text('data_types').notNull(), protocol: text('protocol'), authentication: text('authentication'), direction: text('direction').notNull().default('none'), authorizationRequirements: text('authorization_requirements'), agreementStatus: text('agreement_status').notNull().default('Not Started'), securityReview: text('security_review').notNull().default('Not Started'), privacyReview: text('privacy_review').notNull().default('Not Started'), lastValidation: integer('last_validation'), status: text('status').notNull().default('Candidate')}, t=>[index('idx_connector_registry_org').on(t.organizationId),]);
export const vendorRegistry = sqliteTable('vendor_registry', { id: text('id').primaryKey(), createdAt: integer('created_at').notNull(), organizationId: text('organization_id').notNull().references(()=>organizations.id), name: text('name').notNull(), processorLocation: text('processor_location'), agreementEvidence: text('agreement_evidence'), privacyReview: text('privacy_review').notNull().default('Not Started'), securityReview: text('security_review').notNull().default('Not Started')}, t=>[index('idx_vendor_registry_org').on(t.organizationId),]);
export const policyRegistry = sqliteTable('policy_registry', { id: text('id').primaryKey(), createdAt: integer('created_at').notNull(), organizationId: text('organization_id').notNull().references(()=>organizations.id), title: text('title').notNull(), version: integer('version').notNull(), reviewer: text('reviewer'), evidence: text('evidence'), status: text('status').notNull().default('Under Review')}, t=>[index('idx_policy_registry_org').on(t.organizationId),]);
export const retentionPolicies = sqliteTable('retention_policies', { id: text('id').primaryKey(), createdAt: integer('created_at').notNull(), organizationId: text('organization_id').notNull().references(()=>organizations.id), resourceType: text('resource_type').notNull(), retentionDays: integer('retention_days'), legalHold: integer('legal_hold',{mode:'boolean'}).notNull().default(true), reviewer: text('reviewer'), status: text('status').notNull().default('Under Review')}, t=>[index('idx_retention_policies_org').on(t.organizationId),]);
export const aiModelRegistry = sqliteTable('ai_model_registry', { id: text('id').primaryKey(), createdAt: integer('created_at').notNull(), organizationId: text('organization_id').notNull().references(()=>organizations.id), model: text('model').notNull(), vendorId: text('vendor_id').references(()=>vendorRegistry.id), allowedUses: text('allowed_uses').notNull(), status: text('status').notNull().default('disabled')}, t=>[index('idx_ai_model_registry_org').on(t.organizationId),]);
export const aiPromptRegistry = sqliteTable('ai_prompt_registry', { id: text('id').primaryKey(), createdAt: integer('created_at').notNull(), organizationId: text('organization_id').notNull().references(()=>organizations.id), modelId: text('model_id').notNull().references(()=>aiModelRegistry.id), version: integer('version').notNull(), sourceRecords: text('source_records'), reviewer: text('reviewer'), status: text('status').notNull().default('Under Review')}, t=>[index('idx_ai_prompt_registry_org').on(t.organizationId),]);
export const riskRegister = sqliteTable('risk_register', { id: text('id').primaryKey(), createdAt: integer('created_at').notNull(), organizationId: text('organization_id').notNull().references(()=>organizations.id), asset: text('asset').notNull(), threat: text('threat').notNull(), likelihood: text('likelihood').notNull(), impact: text('impact').notNull(), mitigation: text('mitigation'), owner: text('owner'), reviewAt: integer('review_at'), status: text('status').notNull().default('open')}, t=>[index('idx_risk_register_org').on(t.organizationId),]);
export const governanceEvidence = sqliteTable('governance_evidence', { id: text('id').primaryKey(), createdAt: integer('created_at').notNull(), organizationId: text('organization_id').notNull().references(()=>organizations.id), category: text('category').notNull(), title: text('title').notNull(), owner: text('owner'), reviewer: text('reviewer'), evidence: text('evidence'), reviewedAt: integer('reviewed_at'), nextReviewAt: integer('next_review_at'), status: text('status').notNull().default('Not Started')}, t=>[index('idx_governance_evidence_org').on(t.organizationId),check('evidence_for_completion',sql`${t.status} NOT IN ('Approved','Validated','Production Authorized') OR (${t.owner} IS NOT NULL AND ${t.reviewer} IS NOT NULL AND ${t.owner} <> ${t.reviewer} AND ${t.evidence} IS NOT NULL AND ${t.reviewedAt} IS NOT NULL AND ${t.nextReviewAt} > ${t.reviewedAt})`),]);

export const rolePermissions = sqliteTable('role_permissions',{id:text('id').primaryKey(),role:text('role').notNull(),permission:text('permission').notNull()},t=>[index('idx_role_permissions_role').on(t.role)]);
export const mfaFactors=sqliteTable('mfa_factors',{id:text('id').primaryKey(),userId:text('user_id').notNull().references(()=>users.id),kind:text('kind').notNull(),secret:text('secret'),publicKey:text('public_key'),counter:integer('counter').notNull().default(0),lastStep:integer('last_step').notNull().default(-1),active:integer('active').notNull().default(0),createdAt:integer('created_at').notNull()});
export const mfaChallenges=sqliteTable('mfa_challenges',{id:text('id').primaryKey(),userId:text('user_id').notNull().references(()=>users.id),sessionId:text('session_id').notNull().references(()=>sessions.id),kind:text('kind').notNull(),challenge:text('challenge').notNull(),expiresAt:integer('expires_at').notNull(),usedAt:integer('used_at')});
export const mfaRecovery=sqliteTable('mfa_recovery',{id:text('id').primaryKey(),userId:text('user_id').notNull().references(()=>users.id),hash:text('hash').notNull().unique(),usedAt:integer('used_at')});
export const mfaAttempts=sqliteTable('mfa_attempts',{id:text('id').primaryKey(),userId:text('user_id').notNull().references(()=>users.id),at:integer('at').notNull()});
export const protocolRegistry=sqliteTable('protocol_registry',{id:text('id').primaryKey(),organizationId:text('organization_id').notNull().references(()=>organizations.id),version:integer('version').notNull(),effectiveFrom:integer('effective_from').notNull(),effectiveTo:integer('effective_to'),owner:text('owner').notNull(),approver:text('approver'),approvalStatus:text('approval_status').notNull().default('Human Review Required'),reviewDate:integer('review_date').notNull(),changeHistory:text('change_history').notNull(),label:text('label').notNull().default('Synthetic fixture — not clinically approved')});
export const clinicalHazards=sqliteTable('clinical_hazards',{id:text('id').primaryKey(),organizationId:text('organization_id').notNull().references(()=>organizations.id),hazard:text('hazard').notNull(),cause:text('cause').notNull(),effect:text('effect').notNull(),mitigation:text('mitigation').notNull(),owner:text('owner').notNull(),status:text('status').notNull().default('Human Review Required'),reviewDate:integer('review_date').notNull()});
export const availability=sqliteTable('availability',{id:text('id').primaryKey(),organizationId:text('organization_id').notNull().references(()=>organizations.id),clinicId:text('clinic_id').notNull().references(()=>clinics.id),providerId:text('provider_id').notNull().references(()=>providers.id),startsAt:integer('starts_at').notNull(),endsAt:integer('ends_at').notNull(),timezone:text('timezone').notNull().default('America/Regina'),version:integer('version').notNull().default(1)});
export const appointments=sqliteTable('appointments',{id:text('id').primaryKey(),organizationId:text('organization_id').notNull().references(()=>organizations.id),clinicId:text('clinic_id').notNull().references(()=>clinics.id),patientId:text('patient_id').notNull().references(()=>patients.id),slotId:text('slot_id').notNull().references(()=>availability.id),actor:text('actor').notNull().references(()=>users.id),idempotencyKey:text('idempotency_key').notNull(),originalSlot:text('original_slot').notNull(),relationshipId:text('relationship_id').notNull().references(()=>careRelationships.id),status:text('status').notNull().default('booked'),version:integer('version').notNull().default(1),label:text('label').notNull().default('Demo'),createdAt:integer('created_at').notNull(),updatedAt:integer('updated_at').notNull()});
