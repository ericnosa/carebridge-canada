export async function requestBody(request:Request,origin:string){if(request.headers.get('origin')!==origin||!request.headers.get('content-type')?.startsWith('application/json'))throw Error('INVALID_REQUEST');const reader=request.body?.getReader();if(!reader)throw Error('INVALID_REQUEST');let bytes=0,text='';const decoder=new TextDecoder();while(true){const r=await reader.read();if(r.done)break;bytes+=r.value.byteLength;if(bytes>32768){await reader.cancel();throw Error('INVALID_REQUEST');}text+=decoder.decode(r.value,{stream:true});}return JSON.parse(text+decoder.decode());}
