export function base32(bytes:Uint8Array){const alphabet='ABCDEFGHIJKLMNOPQRSTUVWXYZ234567';let bits=0,value=0,out='';for(const b of bytes){value=(value<<8)|b;bits+=8;while(bits>=5){out+=alphabet[(value>>>(bits-5))&31];bits-=5;}}if(bits)out+=alphabet[(value<<(5-bits))&31];return out;}
export function unbase32(s:string){let bits=0,value=0;const out=[];for(const c of s){const i='ABCDEFGHIJKLMNOPQRSTUVWXYZ234567'.indexOf(c);if(i<0)throw Error('INVALID_SECRET');value=(value<<5)|i;bits+=5;if(bits>=8){out.push((value>>>(bits-8))&255);bits-=8;}}return new Uint8Array(out);}
export async function totp(secret:string,step:number,digits=6){const key=await crypto.subtle.importKey('raw',unbase32(secret),{name:'HMAC',hash:'SHA-1'},false,['sign']);const b=new ArrayBuffer(8);new DataView(b).setBigUint64(0,BigInt(step));const mac=new Uint8Array(await crypto.subtle.sign('HMAC',key,b));const o=mac[mac.length-1]&15;const n=((mac[o]&127)<<24)|(mac[o+1]<<16)|(mac[o+2]<<8)|mac[o+3];return String(n%10**digits).padStart(digits,'0');}
export function equal(a:string,b:string){let n=a.length^b.length;for(let i=0;i<Math.max(a.length,b.length);i++)n|=(a.charCodeAt(i)||0)^(b.charCodeAt(i)||0);return n===0;}
export async function encrypt(secret:string,keyHex:string){if(!/^[a-f0-9]{64}$/i.test(keyHex))throw Error('MFA_KEY_REQUIRED');const key=await crypto.subtle.importKey('raw',Uint8Array.from(keyHex.match(/../g)!,h=>parseInt(h,16)),'AES-GCM',false,['encrypt']);const iv=crypto.getRandomValues(new Uint8Array(12));const bytes=new Uint8Array(await crypto.subtle.encrypt({name:'AES-GCM',iv},key,new TextEncoder().encode(secret)));return JSON.stringify([Array.from(iv),Array.from(bytes)]);}
export async function decrypt(value:string,keyHex:string){if(!/^[a-f0-9]{64}$/i.test(keyHex))throw Error('MFA_KEY_REQUIRED');const [iv,bytes]=JSON.parse(value);const key=await crypto.subtle.importKey('raw',Uint8Array.from(keyHex.match(/../g)!,h=>parseInt(h,16)),'AES-GCM',false,['decrypt']);return new TextDecoder().decode(await crypto.subtle.decrypt({name:'AES-GCM',iv:new Uint8Array(iv)},key,new Uint8Array(bytes)));}
